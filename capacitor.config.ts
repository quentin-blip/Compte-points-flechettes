import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dartsmaster.scorer',
  appName: 'DartsMaster Scorer',
  webDir: 'dist'
};

export default config;
